[project]
name = "btrfs-forensics"
version = "0.1.0"
description = "Brute-force forensic scanner for recovering deleted files from raw Btrfs disk images"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []
