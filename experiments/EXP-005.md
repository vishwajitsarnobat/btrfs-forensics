# EXP-005 — Does copy-on-write zero node slack? What slack holds, and whether C1 may use it

- **Date:** 2026-09-21 (registration and results)
- **Milestone:** M4, first item (plan.md M4 "Settle whether copy-on-write zeroes node slack");
  claim C1 of the paper draft; open question 4 of research.md §10.13
- **Script:** [`experiments/exp005.py`](exp005.py); environment: [`experiments/env.sh`](env.sh)
- **Raw outputs:** `images/scratch/exp/EXP-005/` (gitignored): `results.jsonl` (one line per image),
  `table.txt`
- **Registration:** §1 and §2 were committed before the script existed and before the slack of
  any block of any image was looked at. The commit that adds §6 comes after it in the history.
- **The prototype's row (P4) is historical (added 2026-09-22).** The prototype is our own earlier
  code, not a baseline, so its row says what the rewrite changed and nothing about the field;
  the paper cites the btrfska columns and the independent oracles only. `legacy/` was deleted on
  2026-09-22; the script checks the prototype out from the git tag `legacy-final`
  (`experiments/prototype.py`), so the row stays regenerable.

## 1. Hypothesis

Claim C1 names three recovery sources inside a tree block: items beyond `nritems`, node slack, and
the kernel's ORPHAN_ITEM. The first two live in the block's **slack**: the bytes no current item or
key pointer uses.

- Internal node: everything after the last key pointer, `[101 + 33 × nritems, nodesize)`.
- Leaf: the gap between the item array, which grows up from the header, and the item data, which
  grows down from the end of the block: `[101 + 25 × nritems, 101 + offset of the last item's
  data)`; the whole block after the header when `nritems` is 0.

Toolan & Humphries 2026 hid a message in internal-node slack and lost it: "the slack space is not
copied ... slack space is overwritten by zeros at each copy". They say nothing about leaves. The
prototype (`legacy/utils/btree.py`) mines both kinds of slack, after Bhat & Wani 2018.

**What the kernel does**, read at tag v7.0 before any measurement:

1. **Copy-on-write copies the whole block, slack included.** `btrfs_cow_block`
   (`fs/btrfs/ctree.c:646`) hands the work to `btrfs_force_cow_block` (`ctree.c:464`), which
   allocates the new block and calls `copy_extent_buffer_full(cow, buf)` (`ctree.c:511`).
   `copy_extent_buffer_full` (`fs/btrfs/extent_io.c:4197`) copies `src->len` bytes, the full
   nodesize. A new block is not cleared either: `btrfs_init_new_buffer` zeroes only the header
   (`fs/btrfs/extent-tree.c:5303`). So the mechanism Toolan & Humphries give ("not copied") is not
   what the source says.
2. **The write path zeroes the slack of every tree block.** `write_one_eb` calls
   `prepare_eb_write` (`extent_io.c:2215`) before it submits the block. Under the comment "Set
   btree blocks beyond nritems with 0 to avoid stale content" it zeroes, for an internal node,
   from the end of key pointer `nritems` to the end of the block (`extent_io.c:2227`), and for a
   leaf, from the end of item `nritems` to the start of the last item's data
   (`extent_io.c:2239`): exactly the two ranges defined above. The checksum is computed afterwards
   (`btree_csum_one_bio`, `fs/btrfs/disk-io.c:255`), so it covers the zeroed block.
3. **This is old.** The zeroing came with two commits by Liu Bo, "Btrfs: memset to avoid stale
   content in btree node block" (`3eb548ee3a80`) and "... in btree leaf" (`851cd173f060`), both of
   September 2016. Both are ancestors of v4.9 and not of v4.8 (checked with GitHub's compare API).

So their observation should be right, their explanation wrong, and the effect wider than they
report: not only internal nodes and not only the copy, but every tree block the kernel writes,
leaf or node, first write or rewrite, has all-zero slack on disk. A superseded block was written
by the same path as a live one, so it has nothing in its slack either.

**Registered prediction.** For every image of the corpus and for `sandbox.img`:

- **P1 (kernel-written blocks).** Every checksum-valid tree block has zero non-zero bytes in its
  slack: leaves and internal nodes, live, backup-reachable and unreferenced, inside and outside
  the current chunk map.
- **P2 (superseded against live).** For every pair of a superseded block and its successor (same
  tree, same level, same first key, the next surviving generation), both slacks are all-zero.
  There is nothing to compare: the slack of the superseded copy holds no more than that of the
  live one.
- **P3 (blocks `mkfs.btrfs` wrote).** The blocks of generation 1 come from btrfs-progs 6.6.3, not
  from the kernel, and `prepare_eb_write` never saw them. I predict their slack is zero as well,
  because mkfs builds each block in a cleared buffer. This part is from memory of btrfs-progs,
  **not** from reading its source, and is the likeliest to fail.
- **P4 (the prototype's findings on `sandbox.img`).** research.md §10.13 says the prototype
  "recovered such remnants from `sandbox.img`". If P1 holds that cannot be slack content. I
  predict that the prototype saves no leaf slack and counts no internal-slack residuals on
  `sandbox.img`, and that every "orphan item" it reports beyond `nritems` is a false positive: a
  25-byte window that lies inside the zeroed gap cannot parse, so any hit must come from slots
  that run past the gap into live item data (the prototype scans up to 200 slots beyond
  `nritems` without checking where the item data begins).

**What would refute it.** One checksum-valid block with a non-zero slack byte on an image that
only the pinned mkfs and the guest kernel 7.0 have written (P1, P3). The mutated images
(`corpus/mutate.py`) count too: a mutation that lands inside a block breaks its checksum, and only
valid blocks are measured. An image btrfska refuses at the feature gate is recorded as refused.
For P4: a saved leaf-slack file, a
non-zero residual count, or a beyond-`nritems` hit whose 25 bytes lie inside the gap.

**What the outcome means for the paper.** If P1 and P2 hold:
- "Node slack" and "items beyond `nritems`" are **not recovery sources** on any filesystem written
  by Linux 4.9 (December 2016) or later. C1 can keep them only for filesystems last written by an
  older kernel, which this corpus cannot produce, and must say so. What C1 keeps in full are the
  whole superseded blocks (orphan nodes) and the kernel's ORPHAN_ITEM.
- Bhat & Wani 2018's orphan items would then be an artifact of pre-4.9 kernels. Which kernel they
  used is to be checked in their paper before the draft says so.
- Non-zero slack in a checksum-valid block becomes a **tampering signal** (C5): the kernel cannot
  have written it. That covers Toolan & Humphries' internal-node technique and Wani et al.'s W1
  and W2. It also explains why their hidden message is lost on the next write of that node, and
  predicts the same for leaf slack.
- The M4 slack parser is still worth building, as a detector and for old filesystems, but it
  needs a test image with planted slack, since no honest image has any.

If P1 fails, the blocks that carry slack content are described (tree, level, writer, what the
bytes parse as), and C1 stands for those.

## 2. Method

**Images.** Every row of `corpus/manifest.tsv` as built by `corpus/build.py` on this host (15),
plus `sandbox.img`. All are read through `substrate/image.py`, read-only. `m3_wide` is the only
image with internal nodes; its numbers are reported on their own.

**Blocks.** `discover_image(img, fs, full_sweep=True)`: every sector-aligned candidate of the whole
image, so blocks in removed chunks are included. A block counts when it validates (checksum,
fsid, level, `nritems`, layout). Each **physical copy** is measured, because DUP copies are
written separately; the tables count distinct logical blocks (bytenr, generation, level, owner)
and say when two copies of one block differ.

**Slack metric.** For each valid copy: the slack range as defined in §1, its length, and the
number of non-zero bytes in it. For a leaf the start of the item data is the smallest item data
offset (on a valid leaf that is the last item's). A block with non-zero slack is recorded in full:
image, physical offset, bytenr, generation, owner, level, `nritems`, the offsets of the first and
last non-zero byte, and the first 64 non-zero-run bytes in hex.

**Classes.** Each block is reported by level (leaf, internal), by reachability (`live`,
`backup_reachable`, `unreferenced`, from the scan's classification), by placement (inside or
outside the current chunk map) and by writer (generation 1: `mkfs.btrfs`; above 1: the kernel).

**Pairs.** Within one image, valid blocks are grouped by (owner, level, first key). Inside a group,
ordered by generation, each block and the next one form a superseded-successor pair. For each
pair the two non-zero counts are recorded, and a pair is "live-ended" when its successor is live.
First-key matching misses a block whose first item was deleted between two generations; such a
block is still measured under P1, only not paired.

**The prototype (P4).** `legacy/main.py` runs on `sandbox.img` with its output under
`images/scratch/exp/EXP-005/legacy/`. Recorded: `leaf_slacks_found`, `internal_slack_residuals`
and the orphan-item count from its JSON report. Each beyond-`nritems` hit is then located by the
experiment script: slot range against the leaf's zeroed gap.

**Repetitions.** Parsing a fixed image is deterministic: one run per image, with the image's
hash (plan.md §7). The images come from one build on one host. That limits nothing here if P1
holds on every block, because the claim is about a code path and not about counts; §7 will say
what it does limit.

**Success.** P1 to P4 each hold or fail on their own, and are reported either way.

## 3. Images / scenario

The fifteen rows of `corpus/manifest.tsv`, built once by `corpus/build.py` on the host of §4
(pinned `mkfs.btrfs` 6.6.3, guest kernel 7.0.0-31-generic; nodesize 16 KiB, single device,
metadata DUP, 512 MiB), and `sandbox.img` (256 MiB; the mkfs and kernel that made it are not
recorded). A manifest image has a new filesystem UUID on every build, so its hash identifies this
instance only. `m3_wide` is the only image with internal nodes and the only one without a final
balance.

| Image | SHA-256 of the instance measured |
|---|---|
| `m1_xxhash.img` | `9ee8a560a8952cb123250962db689007a2108995a19739a8721cc2b5d0177fc8` |
| `m1_sha256_bgt.img` | `f2065ce4acaafd7c502085bd56ca2a29d71732d63190c5bce3db4d948a22f47b` |
| `m1_blake2b.img` | `9894ec3aac4337fc851258d180fd488769169e50cf424290fd88f125e6e24e1c` |
| `m1_lzo.img` | `f922d76d095f838a31277ff44fbd15cbba3f25d7bb7f7cf00988b09dfda433bb` |
| `m1_zlib.img` | `69e8735abd140126d42e6bca8b73465ab790e1e2450313447cf83ac4d08307ab` |
| `m1_unknown_incompat.img` | `f94368f2fdb3d9bee6658550330d5617ce499760e047b7fa51d7b7e0816dbc5c` |
| `m1_mirror_damage.img` | `2b2e76f61e3dfcff8ccb70d8e1d7a64462212b1af168cd5caaeb2e68f192920f` |
| `m1_foreign_mirror.img` | `2267d2d3d9be1a0426d328250e52c7920685d7c0f8001a09f897b178f1408742` |
| `m1_badnode.img` | `578a7af212bed694a58fd385b7caf6024d84b438b612e36ac45cf1a5e4249b37` |
| `m1_badnode_both.img` | `11d90e743725cf6db75cab21a594e2eb63096e23009bd373fea425e74dc9ebb1` |
| `m2_logtree.img` | `5324fc126741e51c611539a0d1ad8e86db3f50a799c8ea18fa970e8fd9f69a81` |
| `s01_discard_none_r1.img` | `c0a4241df5d234001c51c01c845b88108ec171aec7d67b3572c44829a1a41783` |
| `s01_discard_async_r1.img` | `3021bcd3f4edbd71af19cf935a99e8820a7b2ebb841068b23652b36fa730ae5a` |
| `s01_discard_sync_r1.img` | `eecf8b2cce509dd5b2df428ec2690558328e2df827fc410d0a751246c9449d8c` |
| `m3_wide.img` | `fa5c52085eb5e6a3c11b544e5ebd5216d2dc1586965d666d8ec1bbcd7513f073` |
| `sandbox.img` | `07ca38d42b11062f5461f97a572134a1b56cbf94e1138183d6e74502f5876418` |

Five **control images** are made and deleted by the script: a 512 MiB file formatted and never
mounted, by the pinned mkfs in the four variants the manifest uses and by the host's
`mkfs.btrfs` 7.1 with its defaults. Every tree block of a control was written by mkfs.

## 4. Environment record

```
date_utc: 2026-09-21T08:22:27Z
host_cpu: 13th Gen Intel(R) Core(TM) i5-1335U
host_logical_cpus: 12
host_ram: 15.3 GiB
images_storage: /dev/nvme0n1p3 (btrfs) on /dev/nvme0n1, model KINGSTON OM8SEP4512Q-AA, rotational 0
host_kernel: 7.2.5-200.fc44.x86_64
qemu: QEMU emulator version 10.2.2 (qemu-10.2.2-1.fc44) (host)
guest_kernel: 7.0.0-31-generic
mkfs_btrfs_progs: mkfs.btrfs, part of btrfs-progs v6.6.3 (pinned)
guest_btrfs_progs: btrfs-progs v6.6.3 (pinned)
host_btrfs_progs: btrfs-progs v7.1 (dump-tree oracle only)
guest_lock_sha256: ef181beb3de4842c72f117d3c2f140064afac10d6a7052c5832994601338f109
python: 3.14.7
uv: uv 0.12.17 (x86_64-unknown-linux-gnu)
uv_lock_sha256: 08dc30c689798ef3014e2dfc2c7098cc0e38ebc409275c697a88bf35eb58f097
git_commit: 3dabb14543cb6a4217ad91b96a0cb2ead92759f9
git_dirty_tracked_files: 0
image_sha256: 07ca38d42b11062f5461f97a572134a1b56cbf94e1138183d6e74502f5876418  sandbox.img
image_sha256: fa5c52085eb5e6a3c11b544e5ebd5216d2dc1586965d666d8ec1bbcd7513f073  images/scenarios/m3_wide.img
```

`git_commit` is the registration commit; the script was written on top of it. The corpus images
matched `images/scenarios/SHA256SUMS` after the run, and `sandbox.img` its tracked hash.

## 5. Exact command

```sh
uv run python corpus/build.py                      # the fifteen images, if not built yet
uv run python experiments/exp005.py run            # controls, then every image; appends results.jsonl
uv run python experiments/exp005.py table          # the tables of §6.1 to §6.3
uv run python experiments/exp005.py legacy         # §6.5
uv run python experiments/exp005.py describe m3_wide.img   # §6.4: one image's non-zero slack, decoded
```

The whole run takes about ten seconds.

## 6. Results

N = 1 per image (deterministic parse of a fixed image); 14 manifest images measured, one refused
at the feature gate (`m1_unknown_incompat`, which is `m1_xxhash` with one superblock bit set),
plus `sandbox.img` and five controls.

### 6.1 The first run refuted P3, and the method had to change

The first run found non-zero slack in 3 to 6 blocks of every image. All of them had a generation
between 2 and 6, the same addresses recurred across images, and none was newer. The registered
method called generation 1 "mkfs" and everything above it "the kernel". That was wrong:
`mkfs.btrfs` does not stop at generation 1. **Added after that first run**, and so not part of the
registration: the never-mounted controls, which measure the generation mkfs leaves behind.

| Never-mounted control | mkfs | sb gen | Blocks | With non-zero slack | Non-zero bytes |
|---|---|---|---|---|---|
| `control_pinned_xxhash.img` (`--csum xxhash`) | btrfs-progs v6.6.3 | 6 | 28 | 8 | 270 |
| `control_pinned_sha256_bgt.img` (`--csum sha256 -O block-group-tree`) | btrfs-progs v6.6.3 | 6 | 31 | 8 | 309 |
| `control_pinned_blake2b.img` (`--csum blake2`) | btrfs-progs v6.6.3 | 6 | 28 | 8 | 266 |
| `control_pinned_crc32c.img` (`--csum crc32c`) | btrfs-progs v6.6.3 | 6 | 28 | 8 | 270 |
| `control_host_default.img` (defaults) | btrfs-progs v7.1 | 8 | 36 | 9 | 481 |

The pinned mkfs ends at generation **G = 6** in every variant; the kernel's first transaction is
7. A block of generation 6 or below was written by mkfs, a block above it by the kernel. The byte
counts of a control move by a few bytes between runs (each format has a new UUID and time); the
block counts and the generation did not. `mkfs.btrfs` 7.1 ends at generation 8.

**P3 is refuted.** mkfs leaves stale content in the slack of about a third of the blocks it
writes (8 of 28), in both versions tried.

### 6.2 Kernel-written blocks: P1 and P2 hold

| Image | sb gen | G | Valid blocks (copies) | Generation ≤ G (mkfs): blocks, with non-zero slack | Generation > G (kernel): leaves, with non-zero slack | internal nodes, with non-zero slack | slack bytes examined, non-zero | pairs leaf / internal (ending in a live block), with non-zero slack |
|---|---|---|---|---|---|---|---|---|
| `m1_xxhash.img` | 38 | 6 | 188 (362) | 24, 6 | 164, 0 | 0, 0 | 2367692, 0 | 128 / 0 (8), 0 |
| `m1_sha256_bgt.img` | 38 | 6 | 205 (395) | 26, 6 | 179, 0 | 0, 0 | 2594960, 0 | 141 / 0 (9), 0 |
| `m1_blake2b.img` | 38 | 6 | 188 (362) | 24, 6 | 164, 0 | 0, 0 | 2365244, 0 | 127 / 0 (8), 0 |
| `m1_lzo.img` | 38 | 6 | 188 (362) | 24, 6 | 164, 0 | 0, 0 | 2363480, 0 | 128 / 0 (8), 0 |
| `m1_zlib.img` | 38 | 6 | 188 (362) | 24, 6 | 164, 0 | 0, 0 | 2364341, 0 | 127 / 0 (8), 0 |
| `m1_unknown_incompat.img` | – | – | not measured: refused at the feature gate | – | – | – | – | – |
| `m1_mirror_damage.img` | 38 | 6 | 188 (362) | 24, 6 | 164, 0 | 0, 0 | 2367692, 0 | 128 / 0 (8), 0 |
| `m1_foreign_mirror.img` | 38 | 6 | 188 (362) | 24, 6 | 164, 0 | 0, 0 | 2367692, 0 | 128 / 0 (8), 0 |
| `m1_badnode.img` | 38 | 6 | 188 (361) | 24, 6 | 164, 0 | 0, 0 | 2367692, 0 | 128 / 0 (8), 0 |
| `m1_badnode_both.img` | 38 | 6 | 187 (360) | 24, 6 | 163, 0 | 0, 0 | 2354583, 0 | 127 / 0 (7), 0 |
| `m2_logtree.img` | 8 | 6 | 47 (80) | 24, 6 | 23, 0 | 0, 0 | 349831, 0 | 6 / 0 (6), 0 |
| `s01_discard_none_r1.img` | 38 | 6 | 188 (362) | 24, 6 | 164, 0 | 0, 0 | 2367692, 0 | 127 / 0 (8), 0 |
| `s01_discard_async_r1.img` | 38 | 6 | 188 (362) | 24, 6 | 164, 0 | 0, 0 | 2367642, 0 | 128 / 0 (8), 0 |
| `s01_discard_sync_r1.img` | 38 | 6 | 26 (38) | 15, 3 | 11, 0 | 0, 0 | 166085, 0 | 0 / 0 (0), 0 |
| `m3_wide.img` | 15 | 6 | 250 (486) | 24, 6 | 192, 0 | 34, 0 | 2055667, 0 | 61 / 16 (25), 0 |
| **14 images** | | 6 | 2407 (4616) | 329, 81 | 2044, 0 | 34, 0 | 28820293, 0 | 1484 / 16 (119), 0 |
| `sandbox.img` (G taken as its highest generation with non-zero slack) | 14 | 8 | 52 (84) | 25, 3 | 27, 0 | 0, 0 | 409475, 0 | 14 / 0 (5), 0 |

The mutated `m1_*` images are copies of `m1_xxhash` and repeat its numbers; they are not
independent evidence. Independent builds: `m1_xxhash`, `m1_sha256_bgt`, `m1_blake2b`, `m1_lzo`,
`m1_zlib`, `m2_logtree`, the three `s01` images and `m3_wide` (10).

| Kernel-written blocks of the manifest images | Blocks | With non-zero slack | Slack bytes | Non-zero bytes |
|---|---|---|---|---|
| leaf, live, inside the map | 247 | 0 | 3124645 | 0 |
| leaf, backup_reachable, inside the map | 156 | 0 | 2142248 | 0 |
| leaf, unreferenced, inside the map | 879 | 0 | 11933178 | 0 |
| leaf, unreferenced, outside the map | 762 | 0 | 11098247 | 0 |
| internal, live, inside the map | 3 | 0 | 46539 | 0 |
| internal, backup_reachable, inside the map | 6 | 0 | 93540 | 0 |
| internal, unreferenced, inside the map | 25 | 0 | 381896 | 0 |

- **P1 holds for every block the kernel wrote**: 2 078 blocks (2 044 leaves, 34 internal nodes),
  28.8 MB of slack, not one non-zero byte; live, backup-reachable and unreferenced, inside and
  outside the current chunk map, log-tree blocks included (`m2_logtree`). As registered ("every
  checksum-valid tree block") P1 is false, because of the mkfs-written blocks of §6.1.
- **P2 holds.** 1 484 leaf pairs and 16 internal-node pairs of kernel-written blocks, 119 of them
  ending in a live block: both slacks are empty in every pair. The internal-node pairs all come
  from `m3_wide`.
- No block has physical copies that differ in slack.

### 6.3 The kernel's rewrite erases what mkfs left

24 pairs on 13 images have an mkfs-written predecessor **with** stale slack content and a
kernel-written successor. The successor's slack is empty in all 24. Example, on every `m1` image:
the chunk-tree leaf of generation 6 carries 70 non-zero slack bytes; its successor of generation
17 carries none. This is Toolan & Humphries' observation reproduced on leaves, without planting
anything: copy-on-write copies the stale bytes into the new buffer (`copy_extent_buffer_full`),
and the write path clears them before the block reaches the disk (`prepare_eb_write`).

An mkfs-written block keeps its stale content for as long as the kernel does not rewrite it. On
`m3_wide` the chunk-tree leaf of generation 6 is still **live** at superblock generation 15, with
its 70 bytes. On `sandbox.img` two live leaves carry such content (chunk tree, generation 8; UUID
tree, generation 7).

### 6.4 What the stale content is

Read as item headers from the start of the slack up to the first all-zero slot, the non-zero
slack of all records decodes to: BLOCK_GROUP_ITEM 168, TREE_BLOCK_REF 106, METADATA_ITEM 94,
CHUNK_ITEM 38, DEV_EXTENT 36, FREE_SPACE_EXTENT 18. These are genuine **items beyond `nritems`**
in Bhat & Wani's sense: well-formed headers of items that mkfs inserted and removed again while
it built the filesystem, some with their data still in place, a few from another tree whose
buffer was reused. None of it concerns a file: there are no files when mkfs runs. No INODE_ITEM,
INODE_REF, DIR_ITEM, DIR_INDEX or EXTENT_DATA header appears in any slack of any image.

### 6.5 The prototype on `sandbox.img` (P4)

| Prototype counter | Value |
|---|---|
| `orphan_items_found` | 0 |
| `internal_orphan_ptrs_found` | 0 |
| `internal_slack_residuals` | 0 |
| `leaf_slacks_found` | 4 |

The four saved files are both DUP copies of two leaves: the chunk-tree leaf of generation 8
(70 non-zero bytes of 15 781) and the UUID-tree leaf of generation 7 (135 of 16 250). Both are
among the three blocks §6.2 lists for `sandbox.img`; the third, an extent-tree leaf of generation
3, goes through the prototype's extent-tree parser, which does not look at slack.

**P4 is half right.** The prototype reports no orphan item and no internal-slack residual, as
predicted; there are not even false positives, because its filter accepts only file-tree item
types and mkfs remnants have none. But it does save leaf slack, which I predicted it would not:
the mkfs remnants of §6.4. The sentence in research.md §10.13, that the prototype "recovered such
remnants from `sandbox.img`" as evidence of deleted items, is wrong: what it saved is mkfs's own
leftovers, and nothing in them belongs to a file. `sandbox.img` was unchanged by the run.

### 6.6 Deviations from the registered method

- The script uses `scan_image(..., full_sweep=True)` instead of `discover_image`: the same scan
  plan and candidate stream, with the reachability classification the method asks for.
- The writer classes were changed after the first run (§6.1): "mkfs" is generation ≤ G from a
  never-mounted control, not generation 1. For `sandbox.img` no control exists, because its mkfs
  is unknown. Its row uses its own highest generation with non-zero slack (8) as G, which is
  circular for that image; it agrees with `mkfs.btrfs` 7.1 ending at generation 8, and
  `sandbox.img` has the block-group tree, a default since 6.19.
- The pairs with an mkfs-written predecessor (§6.3) were not registered. They are reported because
  they are the only place where non-zero slack meets a kernel rewrite.

## 7. Threats to validity

- **Internal.** One build of each image on one host. The claim is about a code path, and no
  kernel-written block deviates, so counts that vary between builds do not touch it. Only 34
  kernel-written internal nodes exist, all on one image; the node and leaf branches of
  `prepare_eb_write` are separate lines of code, so the node result rests on that one image plus
  the source. First-key pairing misses blocks whose first item changed; they are still in P1.
- **External.** One kernel (Ubuntu 7.0.0-31), one nodesize (16 KiB), single device, no zoned
  device, no RAID56, no filesystem older than this year. The source reading says the zeroing is
  unconditional since v4.9; kernels between 4.9 and 7.0 were not run. A filesystem last written
  by a kernel older than 4.9 should still carry slack content: not measured, the corpus cannot
  produce one. Other writers than mkfs in btrfs-progs (`btrfs check --repair`, `btrfstune`,
  `btrfs-convert`, `btrfs rescue`) probably share mkfs's write path and would leave stale slack
  too: **UNVERIFIED**, neither read nor measured.
- **Construct.** "Non-zero bytes" finds any content but does not prove that zero slack never held
  data that happened to be zero; for recovery that distinction is empty. The generation rule for
  the writer assumes nobody set a generation by hand; on a tampered image it proves nothing, and
  that is exactly the case the hiding detector is for.

## 8. Status

**P1 and P2 supported for kernel-written blocks; P3 refuted; P4 half refuted.**

1. Toolan & Humphries' observation is right and general: on Linux 4.9 and later, slack content
   does not survive a write of the block, in internal nodes and in leaves. Their mechanism is
   wrong: the copy includes the slack; the write path zeroes it (`extent_io.c:2215`).
2. **C1 changes.** Node slack and items beyond `nritems` are not a recovery source for deleted
   files on any filesystem the kernel has written since December 2016. On this corpus they hold
   mkfs leftovers and nothing else. C1 keeps: whole superseded blocks (orphan nodes, which the
   scan already finds: 1 828 unreferenced or backup-reachable kernel-written blocks here against
   250 live ones) and the kernel's ORPHAN_ITEM. Slack mining stays claimable only for pre-4.9
   filesystems, as prior work (Bhat & Wani 2018) and untested by us. Bhat & Wani do not name their
   kernel; their dataset paper of the same year (Wani & Bhat 2018) used Fedora 23 with kernel 4.2,
   which predates the zeroing. That their orphan items depend on a pre-4.9 kernel is therefore
   likely, and **UNVERIFIED**.
3. **C5 gains a rule with no false positive on this corpus once mkfs is accounted for:** a
   checksum-valid block with non-zero slack was not written by the kernel. Either a btrfs-progs
   tool wrote it (then the content parses as stale btrfs items, and for mkfs the generation is at
   most the mkfs generation) or somebody rewrote the block and its checksum. A detector that flags
   every non-zero slack would raise 3 to 6 false alarms on every honest image here; it has to
   recognise mkfs remnants.
4. **For M4.** The slack and beyond-`nritems` parser is still built (plan.md M4): as the C5
   detector's input, for old filesystems, and to label mkfs remnants for what they are. It cannot
   be golden-tested against prototype findings of deleted-file items, because there are none; its
   real-data test is the mkfs remnants, and a planted-slack image covers the tampering case. The
   M4 definition of done, "(c) orphan items recover a file", has to mean the kernel's
   ORPHAN_ITEM, not items beyond `nritems`.

Follow-ups: read btrfs-progs' tree-block write path to confirm why mkfs leaves stale content
(and which other tools do); a pre-4.9 guest kernel as a corpus scenario, if the paper wants to
claim slack recovery for old filesystems at all.
