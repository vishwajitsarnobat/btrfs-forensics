# EXP-008 — Joining orphan blocks: what the orphan graph adds, and whether any join is wrong

- **Date:** 2026-09-21 (registration, amendment and results)
- **Milestone:** M5c-1 (plan.md "M5c: the orphan graph"); claim C1 (orphan sources), and the
  third source of C3's timelines
- **Script:** [`experiments/exp008.py`](exp008.py); environment: [`experiments/env.sh`](env.sh)
- **Raw outputs:** `images/scratch/exp/EXP-008/results.jsonl` (gitignored), one line per build
- **Registration:** §1 and §2 were committed before any join was implemented. **Not blind:** a
  survey of one `m4_deep` build on the same day (plan.md M5c) had shown that 303 of 345 orphan
  leaves are named by a scanned key pointer, that 110 file-tree nodes are named by nothing, and
  that `recover --orphans` leaves 134 files `partial` with `continues_elsewhere` and 324
  `complete` files without a path. No join had been computed.

## 1. Hypothesis

Most of what `recover --orphans` reads leaf by leaf belongs to tree versions that were written
within a transaction and replaced before its commit, and can be joined on the evidence of key
pointers; what remains alone can sometimes be joined to a sibling or given a path; and the rules
of plan.md M5c refuse every join that would be wrong.

- **H1 (pointer joins dominate).** At least 80 % of the orphan leaves of a build lie under a
  fragment (a file-tree block that no pointer, ROOT_ITEM or superblock slot names).
- **H2 (cut files).** Of the files `--orphans` leaves `partial` with `continues_elsewhere`, at
  least half come out `complete` with `--graph`, through a fragment or a `sibling` join.
- **H3 (paths).** Of the `complete` orphan files without a path under `--orphans`, at least half
  have one with `--graph`.
- **H4 (no wrong join).** Registered as zero in every build, for each of three checks:
  (a) a `complete` `orphan_graph` file with the name of a logged file (victim, flash, open-
  unlinked) and another hash than the logged one, size-0 versions aside (EXP-007 §6.2);
  (b) a `complete` `orphan_graph` padding file `pad/pN` whose content is none of those the
  scenario can have written to `pN` (`seq 1 300`, or `seq r (300 + r)` for a round `r` with
  `N ≡ r mod 7 + 1 (mod 7)`);
  (c) an `orphan_graph` file that an anchored root also holds with the same tree, inode,
  creation generation and `transid`, but with another extent signature.
- **H5 (refusals happen and are visible).** Some joins are refused, and every refusal is in the
  artifact's problems with its reason. Registered: at least one refusal per build; no prediction
  of how many.

**What would refute it.** Any count above zero in H4: then a rule lets through a file that never
existed, and `orphan_graph` cannot be offered as evidence until the rule is fixed. H1 to H3
failing would mean the graph adds little on this kind of history, which would be reported as
such.

**What it means for the paper.** C1's orphan sources become usable at file level (names, whole
files), with the provenance of every join; the paper can say how much of an image's
unreferenced metadata is explained by never-committed tree versions.

## 2. Method

**Scenario.** `corpus/vm/scenarios/deep.guest.sh`, the recipe of `m4_deep` (EXP-006 §2): 48
subvolumes, 400 inline padding files of which a seventh is rewritten per round, 24 victims, 8
flash files, one file unlinked while open, power-off without unmount. Every content is
deterministic, so every recovered padding file can be checked against the scenario.

**Builds.** Five fresh builds, each measured and deleted, and the corpus image `m4_deep` as a
sixth line. `m3_wide` (no log tree, deletions between commits) is measured once for H1 to H3.

**Measurement per build.** `catalog build --full-sweep`; `recover --root all --tree all
--orphans` and, into another directory, `recover --root all --tree all --graph`. Orphan leaves
under a fragment are counted from the second run's provenance. Files are matched between the
runs by (tree, inode, creation generation, `transid`, leaf of the INODE_ITEM). H4 is evaluated on
the second run as defined above. The image's hash is taken before and after.

**Reported.** Median and range over the five builds, the host, and any bound that bit.

## 2a. Amendment before the builds (2026-09-21)

A trial of the finished script on the two corpus images, before any of the five builds, showed
that two of the three checks of H4 do not measure what they were meant to. It is recorded here
with its numbers, because the registered checks fail as worded.

- **What the trial showed** (`m4_deep`, one build): H4(a) 0 of 22; **H4(b) 225 of 37 743** padding
  files with a content the scenario cannot have written; **H4(c) 316 of 44 326** files with
  another extent signature than the anchored file of the same inode, creation generation and
  `transid` (`m3_wide`: 192 of 19 334).
- **None of them is a join.** Every one of the 316 is a file whose items lie in a single leaf of
  a fragment. 247 hold an extent that is *newer than their own INODE_ITEM*: the leaf was written
  to disk after the data was rewritten and before the inode item was updated (btrfs updates it
  through delayed items, at the commit at the latest), so the new data is cut to the old size.
  That is what H4(b) found, too: `pad/p298` reads `seq 24 324` cut at the 1 121 bytes of the
  version before. The other 69 are size-0 versions (truncated, not yet rewritten) within the same
  transaction as the anchored version. So (tree, inode, creation generation, `transid`) does not
  identify a version of a file outside committed trees, and H4(c) was built on that assumption.
  M4's `recover --orphans` has read these leaves in the same way since M4d and called them
  `complete`.
- **What changed in the tool, because of it.** A file with an extent newer than its INODE_ITEM is
  no longer `complete`: it is `partial`, reason `inode_item_older_than_extent`, for every source.
  On seven images (every state, deduplication off) no anchored root holds such a file, which is
  what the commit guarantees; only uncommitted leaves do.
- **The checks as amended, registered now, before the builds.** H4(a) is unchanged. **H4(b2):**
  as (b), which now sees such files as `partial`, so it counts only files called `complete`:
  registered 0. **H4(c2):** as (c), but only for `complete` files whose items come from two
  leaves or more, the only files whose *content* a join assembled: registered 0. The registered
  (b) and (c) are still computed and reported; (c) is expected to stay far above zero, for the
  reason above, and that is reported as the refutation of the check, not of the joins.
- H1 to H3 and H5 are unchanged. The trial's values for them (`m4_deep`: 303 of 345 leaves under
  a fragment; 133 of 134 cut files complete; 2 757 of 3 137 pathless files with a path) were seen
  before the builds, so the builds add the spread, not the surprise.

## 3. Image / scenario

`corpus/manifest.tsv` rows `m4_deep` (recipe in EXP-006 §3) and `m3_wide`. Five builds
`exp008_r1` … `exp008_r5` of scenario `deep`, measured and deleted, and the two corpus images of
this host's build: `m4_deep` `6f2c398fefdad3a2f42f3aa83c4a9425629f2640312d0d14f48476b52e7467a0`,
`m3_wide` `fa5c52085eb5e6a3c11b544e5ebd5216d2dc1586965d666d8ec1bbcd7513f073`. All seven images
were unchanged by the measurement.

## 4. Environment record

```
date_utc: 2026-09-21T17:42:56Z
host_cpu: 13th Gen Intel(R) Core(TM) i5-1335U
host_logical_cpus: 12
host_ram: 15.3 GiB
images_storage: /dev/nvme0n1p3 (btrfs) on /dev/nvme0n1, model KINGSTON OM8SEP4512Q-AA, rotational 0
host_kernel: 7.2.5-200.fc44.x86_64
qemu: QEMU emulator version 10.2.2 (qemu-10.2.2-1.fc44) (host)
guest_kernel: 7.0.0-31-generic
mkfs_btrfs_progs: mkfs.btrfs, part of btrfs-progs v6.6.3 (pinned)
guest_btrfs_progs: btrfs-progs v6.6.3 (pinned)
host_btrfs_progs: btrfs-progs v7.1 (dump-tree oracle only)
guest_lock_sha256: ef181beb3de4842c72f117d3c2f140064afac10d6a7052c5832994601338f109
python: 3.14.7
uv: uv 0.12.17 (x86_64-unknown-linux-gnu)
uv_lock_sha256: 08dc30c689798ef3014e2dfc2c7098cc0e38ebc409275c697a88bf35eb58f097
git_commit: 20f5a640bf87c450ae24a950272f360ceafc64ac
git_dirty_tracked_files: 0
```

## 5. Exact command

```sh
uv run python experiments/exp008.py run --builds 5
uv run python experiments/exp008.py run --image images/scenarios/m4_deep.img
uv run python experiments/exp008.py run --image images/scenarios/m3_wide.img
uv run python experiments/exp008.py table
```

## 6. Results

**Two runs, and why.** The five builds were measured twice. The first run (with the rules as
amended in §2a) gave zero for H4(a), (b2) and (c2) on all five builds and on `m4_deep`, but
**(c2) = 2 of 144 on `m3_wide`**: one file, `d/f1445`, in two fragments. Its INODE_ITEM (size 0,
just created) and its inline extent (10 bytes, written a moment later) lie in two leaves of the
same generation, and the fragment's pointers join them: the file comes out `complete` and empty,
while the committed version has 10 bytes. The generations agree, so the first rule of §2a does
not see it. It is the hazard plan.md M5c names ("its blocks were written at different moments of
that transaction"), caught by the check built for it. A second rule was added for blocks that
were never committed: an inline or regular extent that reaches past the sector of the end of the
file means the same as a newer extent, and the file is `partial`. The five builds were then
built and measured again with the final code; the table is that second run. Nothing in the
first run contradicts it (first run: 302 (301–303) of 343 leaves under a fragment, 122 of 134 cut
files, 2 524 of 2 864 paths, H4 as said).

N = 5 fresh builds of scenario `deep`, median (range); then the two corpus images.

| Quantity | five builds | `m4_deep` | `m3_wide` |
|---|---|---|---|
| orphan leaves | 345 (343–345) | 345 | 26 |
| **H1** … under a fragment | 303 (300–303) | 303 | 26 |
| fragments | 110 (108–110) | 110 | 14 |
| **H2** files cut by a leaf end under `--orphans` | 134 (134–135) | 134 | 2 |
| … `complete` under `--graph` | 122 (122–122) | 122 | 2 |
| **H3** `complete` files without a path under `--orphans` | 2 849 (2 799–2 849) | 2 849 | 403 |
| … with a path under `--graph` | 2 509 (2 459–2 509) | 2 509 | 403 |
| artifacts with a `pointer` join | 44 199 (43 908–44 199) | 44 199 | 19 334 |
| artifacts with a `sibling` join | 6 (5–6) | 6 | 0 |
| `parent_path` joins | 148 (146–149) | 148 | 0 |
| **H5** refusals (sibling, path) | 10 (10–11): 2 (2–3), 8 (8–8) | 10: 2, 8 | 0 |
| files with an inode item older than an extent (now `partial`) | 303 (300–303) | 303 | 144 |
| `complete` `orphan_graph` files that no root gives | 33 (33–33) | 33 | 48 |
| **H4(a)** logged files checked, with a wrong hash | 22 (21–22), **0** | 22, 0 | none logged |
| **H4(b2)** padding files checked, with an impossible content | 37 518 (37 116–37 518), **0** | 37 518, 0 | – |
| **H4(c2)** `complete` files from several leaves that a root also holds, with another signature | 2 875 (2 848–2 875), **0** | 2 875, 0 | 142, **0** |
| H4(c) as first registered: files a root also holds, with another signature | 44 326 (44 033–44 326), 316 (313–316) | 316 | 192 |

### 6.1 What the numbers say

- **H1 holds:** 88 % of the orphan leaves hang under a fragment (registered: at least 80 %). What
  `--orphans` reads as hundreds of unrelated leaves is, for the most part, about a hundred tree
  versions per image that were written and replaced within a transaction.
- **H2 holds:** 122 of 134 cut files (91 %) come out `complete`; registered at least half. Most
  of them inside fragments, at most six per build through a `sibling` join. The twelve others have
  a stale inode item or no tail that fits.
- **H3 holds:** 88 % of the pathless files get a path (registered at least half).
- **H4 holds as amended, and as first registered it does not.** No logged file with a wrong
  hash; none of 37 518 padding files with a content the scenario cannot have written; none of
  2 875 multi-leaf files that disagrees with the anchored file of the same identity. H4(b) as
  registered is 0 too, now that stale versions are `partial`. **H4(c) as registered stays at 316:**
  it counts stale and size-0 versions within single leaves, which are no joins (§2a). The check
  was wrong, and the record keeps it.
- **H5 holds:** 10 refusals per build, each with its reason in the artifact.
- **"Complete files that no root gives" fell from 68 to 33** between the runs, and from 144 to 48
  on `m3_wide`: the rest were empty files with data attached. That corrects M4d's catalog entry,
  which counted 145 such versions on `m3_wide` as recovered versions of size 0.

## 7. Threats to validity

- **Internal.** The scenario was known, and its trial results too (§2a): this is not a blind
  test. The two rules about inode items were found *by* H4's checks on these images, so H4 = 0
  afterwards is partly by construction; what is independent is that the rules are general
  statements about btrfs (a commit updates the inode item; it leaves no data past the end of a
  file) and that no anchored root of seven images trips the first one. The padding check knows
  the contents a name can have, not which round a version belongs to. `sibling` joins were few
  (6 per build), and on the corpus image every one of them is a duplicate of a file an anchored
  root gives (same inode, creation generation and extent signature), which confirms them; a
  sibling join that gives a file no root has was not observed, so it is tested on forged leaves
  only.
- **External.** One workload (small inline files rewritten in rounds, explicit `sync`), 512 MiB,
  seconds old, no concurrent writers, stock 7.0 guest. How many fragments a real system leaves
  depends on how often dirty tree blocks are written before a commit. Fragments of trees with
  more than two levels were not seen.
- **Construct.** "Under a fragment" counts leaves, not recovered information: most fragment
  artifacts are duplicates of what roots give (44 199 artifacts, 33 new complete files). The
  graph's value on this image is names and whole files for versions that were never committed,
  and the explanation of what the orphan leaves are.

## 8. Status

**Supports H1, H2, H3, H5 and the amended H4; refutes the check H4(c) as first registered.**
Follow-ups: a workload with larger multi-leaf files and hard links; the second rule's behaviour
on files with preallocated tails (not flagged: prealloc is excluded); log trees (M5c-2).
