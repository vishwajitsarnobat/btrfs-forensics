# EXP-007 — Reading a pre-balance state through the chunk map of its own time

- **Date:** 2026-09-21 (registration and results)
- **Milestone:** M5a, and M5's definition of done ("`s01` yields a reconstructed historical chunk
  map and correctly-translated outside-map orphans"); claim C6
- **Script:** [`experiments/exp007.py`](exp007.py); environment: [`experiments/env.sh`](env.sh)
- **Raw outputs:** `images/scratch/exp/EXP-007/results.jsonl` (gitignored), one line per build
- **Registration:** §1 and §2 were committed before any historical map was built or any file was
  read through one. **What was known beforehand**, so the reader can judge how blind this is:
  - M4b's result on `s01_discard_none_r1`: from a pre-balance state, 3 files come out `partial`,
    every extent `unmapped` (catalog.md, M4b);
  - M2b's counts of blocks that a state's own chunk items place (`states.maps_historical`,
    EXP-002: 26 of 35 states have a chunk root that differs from the current one);
  - a listing, made today while designing M5a, of the CHUNK_ITEMs and DEV_EXTENTs in the scanned
    leaves of `s01_discard_none_r1`: chunk-tree leaves of generations 2 to 6, 17, 23, 24, 29, 30
    and 38 survive, and the old data chunk (logical 13631488) is in every one of them before 38.
  No translation of any block or extent through a historical map had been computed.
- **Correction to the registration (added with the results).** The second item above misquoted
  EXP-002: its "26" counts superblock and backup slot references, not states. How many states have
  a chunk root other than the current one had not been published; this experiment measured it
  (34 of 35 without discard). No prediction rested on the number.

## 1. Hypothesis

After a full balance, the chunk maps of the time before it are still on the disk, and reading
through them gives back what the current map cannot address.

- **H1 (file data, no discard).** On a build without discard, every regular file of a pre-balance
  state that the current map leaves `partial` with `unmapped` extents comes out `complete` when
  read through the state's own map, and every such file the scenario's log names (`keep.txt`,
  `deleted_big.txt`) has the logged SHA-256. Registered: all of them, in 5 of 5 builds; no
  `complete` file with a wrong hash, ever. The reason to expect it: a balance copies the data and
  frees the old chunk without writing to it, and the three new chunks are allocated at higher
  device offsets than the old data chunk.
- **H2 (discard).** With `discard=async` and the quick unmount of this scenario, the result equals
  H1 (EXP-002 found every stale metadata block still in place; the same is expected of the old
  data chunk). This is the less certain prediction: nothing measured so far says what happens to
  a freed *data* chunk under async discard. With `discard=sync` the historical maps gain nothing:
  EXP-002 found only 2 states there, both after the balance began, and the freed chunks are
  trimmed. Registered: async as H1; sync 0 files gained, and no file that is `complete` with a
  wrong hash (a trimmed zstd extent must fail to decode, not decode to zeros).
- **H3 (outside-map blocks).** Every checksum-valid tree block that lies outside the current chunk
  map is placed, at the offset where it was scanned, by at least one historical map, and by the
  map of its own time (the newest surviving chunk root not newer than the block). Registered:
  100 % for both on the no-discard builds. A block's header carries its logical address, so this
  tests the maps against ground truth the maps were not built from.
- **H4 (the second witness).** Every stripe of every chunk in a historical map is confirmed by a
  DEV_EXTENT in some scanned dev-tree leaf, and the map assembled from DEV_EXTENTs alone agrees
  with the CHUNK_ITEM maps on every chunk both know (same logical address, length and stripe
  offsets). Registered: 100 % on the no-discard builds, and no chunk known from DEV_EXTENTs only.

**What would refute it.** A file that reads `complete` through a historical map with a hash other
than the logged one (then "every byte was read" is not enough, and the map or the rule that
picks it is wrong, or the space was overwritten without our check noticing). A valid outside-map
block that no map places (then a chunk-tree generation was lost, and H3's "own time" rule needs
a fallback that the experiment would then have to evaluate separately). A stripe that a
DEV_EXTENT contradicts.

**What it means for the paper.** H1 and H3 together are claim C6 at file level and at block
level. N-numbers in paper-draft.md that rest on "outside the current chunk map" (the 21 of 71
sandbox orphans, the 133 states of EXP-004) get their explanation: those blocks are where an
older map says they should be.

## 2. Method

**Scenario.** `corpus/vm/scenarios/s01.guest.sh`, the recipe of the corpus images
`s01_discard_{none,async,sync}_r1`: pinned `mkfs.btrfs` 6.6.3, guest kernel 7.0.0-31, 512 MiB,
`compress=zstd,commit=5`. A subvolume with `keep.txt`, `deleted_big.txt` and an inline file, their
SHA-256 logged; a read-only snapshot; the two `deleted_*` files removed; six committed one-line
writes; a full balance ("had to relocate 3 out of 3 chunks"); unmount.

**Builds.** Five fresh builds per discard mode (none, async, sync), each measured and deleted; a
guest run is not bit-stable (CONTRIBUTING.md §5). The three corpus images are measured too, as
extra lines, for the record.

**Measurement per build.** `catalog build --full-sweep`; then `recover --root all --tree all`
twice into separate directories, once with `--maps current` (M4's behaviour) and once with
`--maps own`. Artifacts are matched between the two runs by (state, tree, inode, creation
generation).
- *H1/H2:* the regular files that are `partial` with an `unmapped` range under `current`; how
  many of them are `complete` under `own`; of the `complete` ones named `keep.txt` or
  `deleted_big.txt`, how many carry the logged hash; every `complete` file under `own` whose name
  the log knows and whose hash differs is counted separately (expected 0). Files whose status is
  the same in both runs are counted as a check that the maps change nothing else.
- *H3:* from `nodes` (valid, `maps_here` 0) and `node_maps`: blocks placed by some historical map;
  blocks placed by the map of their own time. Each placement is recomputed by the script from
  `chunks` and `stripes`, not taken from `node_maps` on trust.
- *H4:* from `stripes.dev_extents` over the historical maps; and the chunk-by-chunk comparison of
  the `dev_extents` map with the CHUNK_ITEM maps.
The image's hash is taken before and after.

**Reported.** For every quantity the median and the range over the five builds of a mode, and the
host. A bound that bites (`problems`, sources `roots` and `chunk_maps`) is reported per build.

## 3. Image / scenario

`corpus/manifest.tsv` rows `s01_discard_none_r1`, `s01_discard_async_r1`, `s01_discard_sync_r1`
(`NAME=… corpus/vm/scenarios/discard_{none,async,sync}.sh`; the three scripts differ only in
whether the virtio disk passes discards on and in the `discard=` mount option). Fifteen builds
`exp007_{none,async,sync}_r{1..5}` with the same commands, measured and deleted, and the three
corpus images of this host's build:

| Image | SHA-256 |
|---|---|
| `s01_discard_none_r1` | `c0a4241df5d234001c51c01c845b88108ec171aec7d67b3572c44829a1a41783` |
| `s01_discard_async_r1` | `3021bcd3f4edbd71af19cf935a99e8820a7b2ebb841068b23652b36fa730ae5a` |
| `s01_discard_sync_r1` | `eecf8b2cce509dd5b2df428ec2690558328e2df827fc410d0a751246c9449d8c` |

Every build has a new filesystem UUID, so a hash identifies one instance only. All 18 images were
unchanged by the measurement.

## 4. Environment record

```
date_utc: 2026-09-21T16:17:50Z
host_cpu: 13th Gen Intel(R) Core(TM) i5-1335U
host_logical_cpus: 12
host_ram: 15.3 GiB
images_storage: /dev/nvme0n1p3 (btrfs) on /dev/nvme0n1, model KINGSTON OM8SEP4512Q-AA, rotational 0
host_kernel: 7.2.5-200.fc44.x86_64
qemu: QEMU emulator version 10.2.2 (qemu-10.2.2-1.fc44) (host)
guest_kernel: 7.0.0-31-generic
mkfs_btrfs_progs: mkfs.btrfs, part of btrfs-progs v6.6.3 (pinned)
guest_btrfs_progs: btrfs-progs v6.6.3 (pinned)
host_btrfs_progs: btrfs-progs v7.1 (dump-tree oracle only)
guest_lock_sha256: ef181beb3de4842c72f117d3c2f140064afac10d6a7052c5832994601338f109
python: 3.14.7
uv: uv 0.12.17 (x86_64-unknown-linux-gnu)
uv_lock_sha256: 08dc30c689798ef3014e2dfc2c7098cc0e38ebc409275c697a88bf35eb58f097
git_commit: ec255f1d44f0f4533edc11bef5413c38a5750b76
git_dirty_tracked_files: 0
```

## 5. Exact command

```sh
uv run python experiments/exp007.py run --builds 5        # 15 builds: none, async, sync
for m in none async sync; do
  uv run python experiments/exp007.py run --image images/scenarios/s01_discard_${m}_r1.img
done
uv run python experiments/exp007.py table
```

## 6. Results

N = 5 fresh builds per mode, on the host of §4; median (range). The corpus image of each mode
gave the same values as its mode's median in every row. No bound was hit in any build.

| Quantity | none | async | sync |
|---|---|---|---|
| superblock generation | 38 (38–38) | 38 (38–38) | 38 (38–38) |
| states | 35 (35–35) | 35 (35–35) | 2 (2–2) |
| of them with a chunk root other than the current one | 34 (34–34) | 34 (34–34) | 1 (1–1) |
| historical maps (surviving chunk-tree roots besides the current) | 10 (10–10) | 10 (10–10) | 4 (4–4) |
| **H1/H2** file artifacts `partial` with `unmapped` ranges under `--maps current` | 81 (81–81) | 81 (81–81) | 0 (0–0) |
| … `complete` under `--maps own` | 81 (81–81) | 81 (81–81) | 0 |
| … and carrying the logged SHA-256 | 81 (81–81) | 81 (81–81) | 0 |
| distinct logged files behind them | 2 (2–2) | 2 (2–2) | 0 |
| still `partial` under `--maps own` | 0 (0–0) | 0 (0–0) | 0 (0–0) |
| `complete` with a hash other than the logged one | 0 (0–0) | 0 (0–0) | 0 (0–0) |
| size-0 early versions of logged files (see 6.2) | 3 (3–3) | 3 (3–3) | 0 (0–0) |
| other file artifacts, and how many are the same in both runs | 210, 210 | 210, 210 | 10, 10 |
| **H3** valid tree blocks outside the current map | 172 (172–174) | 172 (172–172) | 16 (16–16) |
| … placed by some historical map | all | all | all |
| … placed by the map of their own time | all | all | all |
| … placed by the map from DEV_EXTENTs alone | all | all | all |
| blocks where `node_maps` differs from the script's recount | 0 | 0 | 0 |
| **H4** stripes of historical chunks | 56 (56–56) | 56 (56–56) | 19 (19–19) |
| … confirmed by a DEV_EXTENT | all | all | all |
| chunks known from CHUNK_ITEMs and from DEV_EXTENTs, and agreeing | 8, 8 | 8, 8 | 8, 8 |
| chunks known only one way; DEV_EXTENT chunks rejected | 0; 0 | 0; 0 | 0; 0 |

"All" means equal to the row above it in every build, not only in the median.

### 6.1 What the numbers say

- **H1 holds.** Without discard, everything the current map left `unmapped` reads `complete`
  through the map of its state's own time, and every one of those artifacts has the SHA-256 the
  guest logged before the balance. **The 81 are artifacts, not files:** they are the versions of
  two files, `keep.txt` and `deleted_big.txt`, in the subvolume and in its snapshot, as 34
  pre-balance states hold them (deduplication is off for a file that stays `partial`, which is
  why `--maps current` writes each one again). At file level the result is "2 of 2".
- **H2 holds, both halves.** Async discard with a quick unmount equals no discard, for the freed
  data chunk as for the metadata (EXP-002). Under `discard=sync` there is nothing to gain: two
  states survive, neither holds a file the current map cannot read, and nothing reads `complete`
  with a wrong hash.
- **H3 holds.** Every valid block outside the current map lies where a historical map puts its
  header's address, and the map chosen by "newest surviving chunk root not newer than the block"
  always does. Under sync that is still true of the 16 blocks that survive.
- **H4 holds.** Every historical stripe has a DEV_EXTENT that agrees with it, and the map built
  without any CHUNK_ITEM is the same map: 8 chunks, the same addresses, lengths and offsets.

### 6.2 What was not predicted

- **Size-0 versions.** One early state (the transaction in which the files were created, before
  their data reached the disk) holds `keep.txt`, `deleted_big.txt` and `deleted_inline.txt` at
  size 0. They are `complete` in both runs, with the SHA-256 of the empty string, and they read
  no extent, so no map is involved. The registered check "no `complete` file with a wrong hash"
  would have counted them; the script counts them in a row of their own. They are true
  historical versions, and a reader comparing hashes with a log must expect them.
- **The balance's own chunks.** The three chunks the balance created (metadata 63963136, system
  131072000, data 164626432) appear one by one in the maps of generations 17, 23 and 30; the old
  data chunk 13631488 is still in the map of generation 30 and gone in 38. The order of a balance
  can be read off the maps.

## 7. Threats to validity

- **Internal.** One scenario, 38 generations, seconds old; the old data chunk is never written
  again because nothing is allocated after the balance. On a filesystem that keeps running, the
  freed space is reused, and then a read through a historical map returns newer bytes. The tool
  reports that case only when a newer *map* has allocated the space (tested on forged maps, never
  seen here); reuse inside a chunk that still exists is invisible to the maps, and only a data
  checksum (M6) can catch it. Here the logged hashes stand in for the checksum. The files are
  zstd-compressed, which makes a trimmed extent fail to decode; an uncompressed extent in a
  trimmed chunk would read as zeros and `complete`. No build here had such a file.
- **External.** Single device, SINGLE and DUP profiles only, 512 MiB, a one-leaf chunk tree in
  every generation, stock 7.0 guest, virtio. Multi-device and striped profiles are tested on
  forged items only. A chunk tree with internal nodes whose root is lost would leave orphan chunk
  leaves, which this version does not use (the DEV_EXTENT map is the fallback there). Async
  discard was measured with a quick unmount only (EXP-002 §7).
- **Construct.** "Placed by the map of its own time" uses the block's header generation against
  the chunk roots' generations; with every chunk-tree version surviving, as here, the rule cannot
  fail, so H3's second half is weak evidence for the rule on images where versions are lost.
  H4's agreement is between two trees the same kernel code wrote in the same transaction: it
  checks our reading of them, not the kernel.

## 8. Status

**Supports H1 to H4** (N = 5 per mode, no spread except 172–174 outside-map blocks in the none
builds). Follow-ups: a scenario that keeps writing after the balance, so that freed space is
reused (with M6's checksums); a multi-device image (M7, E-raid); an image whose chunk tree has
internal nodes.
