# EXP-002 — Scanning the discard trio: probe agreement, orphan classes and surviving history

- **Date:** 2026-09-15
- **Milestone:** M2 DoD "discard trio: exact per-image agreement with the probe; cross-run numbers
  reported as median and range next to EXP-000" (plan.md §5), catalog.md M2b entry
- **Script:** [`experiments/exp002.py`](exp002.py); images from [EXP-000](EXP-000.md);
  environment: [`experiments/env.sh`](env.sh)
- **Raw outputs:** `images/scratch/exp/EXP-002/` (gitignored): `results.jsonl` (one line per image:
  probe columns, the compatibility count and its skip-range diff, btrfska's classes, walk failures,
  every evaluated root-tree state), `run_stdout.txt`, `table.txt`
- **Tests:** `tests/test_discard_trio.py` (vm) re-checks the agreement, the classes and the
  discovery results on the three representative images.

## 1. Hypothesis

- **Pre-registered** (plan.md M2): on every discard-trio image, btrfska's full-sweep scan,
  counted under `probe_stale_metadata.py`'s own rules, gives exactly the probe's `fsid_blocks` and
  `stale_blocks`. This is a deterministic parse, so no tolerance applies.
- **Exploratory** (no prediction was registered before the measurement): how many historical
  root-tree states, and how much of each, old-root discovery still finds under each discard mode.
  These results are reported as observations.

## 2. Method

**Images.** The 45 images of EXP-000 (runs 1–15 × none, async, sync), measured before the
cleanup, and, for agreement only, the three earlier `images/scenarios/s01_discard_{none,async,sync}.img`
(generated on 2026-09-15 before M2b by the same scripts, not in the manifest). Every image was
measured once, sequentially, with one worker.

**Agreement.** For each image:
1. `probe_stale_metadata.py IMAGE`, unchanged, as a subprocess;
2. `probe_compat(IMAGE)`: the probe's first two columns computed from btrfska.

First, the ranges each tool reads were diffed. `probe_compat` derives them from the scan plan on
every image rather than assuming them. On a 512 MiB image (131 072 blocks of 4 KiB):

| Block range (4 KiB) | Bytes | Probe | btrfska `--full-sweep` | Why btrfska differs |
|---|---|---|---|---|
| 0–15 | 0x0–0xFFFF | probed | skipped (`reserved`) | The kernel excludes the first 64 KiB and every superblock copy from every block group (block-group.c:2277-2330), so no tree block can lie there |
| 16 | 0x10000–0x10FFF | skipped (primary superblock) | skipped (`reserved`) | — |
| 16384 | 64 MiB | skipped (mirror 1) | skipped (`superblock`) | — |
| 67108864 | 256 GiB | beyond the image | beyond the image | — |
| every other block | | probed | probed (full sweep: chunk stripes, DATA included, and unmapped gaps) | — |

So blocks 0–15 are the only blocks one tool reads and the other does not: 16 blocks, probe only.
No block is read by btrfska only.

**Compatibility count** (`experiments/exp002.py`, `probe_compat`):
- `fsid_blocks` = every btrfska full-sweep candidate (every 4 KiB offset whose 16 bytes at +0x20
  equal the tree fsid; invalid candidates included) + the blocks among 0–15 whose bytes at +0x20
  equal the primary superblock's fsid;
- `stale_blocks` = the candidates whose header generation (+0x50) is below the **primary**
  superblock's generation, valid or not + the same test on those boot-area hits;
- the two remaining definitional differences are checked per image, not assumed: the probe's fsid
  (superblock `fsid`) equals btrfska's tree fsid (no METADATA_UUID), and the primary superblock's
  generation equals the selected copy's. Both held on all 48 images;
- sectorsize is 4096 on every image, so btrfska probes the same offsets as the probe.

Nothing else was adjusted. Columns 3 and 4 (inline-string copies, non-zero blocks) are not tree-block
counts and are reported from the probe only.

**btrfska's own classes** (`scan_image`, full sweep, as `btrfska scan --full-sweep`): candidates,
valid, invalid, `live`, `backup_reachable`, `unreferenced`, valid orphans outside the current
chunk map, and legacy-compatible orphans. Walk failures are counted by class (README, `btrfska
roots`).

**Old-root discovery** (`discover_image`, full sweep, as `btrfska roots --full-sweep`): root-tree
candidate roots; the superblock and backup roots rediscovered (indexed, and candidate roots); every
evaluated state's completeness (found / referenced distinct tree blocks, resolved through the scanned
blocks; see README). A state is *beyond the backups* when neither the superblock nor a backup slot
names its root-tree block.

**Statistics.** Per image all counts are deterministic. Across runs, per mode: median (min–max),
N = 15.

## 3. Images / scenario

As EXP-000 §3: scenario s01, 512 MiB, xxhash, zstd, `commit=5`; stock 7.0.0-31 guest; host
btrfs-progs 6.6.3; representative images `s01_discard_{none,async,sync}_r1` in
`corpus/manifest.tsv`. The 45 per-run SHA-256s are in `results.jsonl` and equal those recorded at
generation (EXP-000 §4).

## 4. Environment record

Same host, session and commit as EXP-000 §4 (`git_commit: 97ffc1f26febf62be0ceb31426f0005fac9fc35a`,
0 dirty tracked files), taken immediately before EXP-000 run 1. EXP-002 ran after run 15 and before
the cleanup. The code measured (`scan/`, `substrate/`) is unchanged between that commit and the
commit that adds this record.

## 5. Exact commands

```sh
# after EXP-000 runs 1-15, before `exp000.py clean`
uv run python experiments/exp002.py run images/scenarios/s01_discard_{none,async,sync}.img
uv run python experiments/exp002.py run images/scenarios/s01_discard_{none,async,sync}_r{1..15}.img
uv run python experiments/exp002.py table
# the representative images, any time later
uv run btrfska scan images/scenarios/s01_discard_sync_r1.img --full-sweep
uv run btrfska roots images/scenarios/s01_discard_sync_r1.img --full-sweep
uv run pytest -m vm tests/test_discard_trio.py
```

## 6. Results

### 6.1 Agreement with the probe (pre-registered)

**48 of 48 images agree exactly** on both columns: the 45 regenerated images and the 3 earlier ones.

| Mode | Images | Probe fsid_blocks / stale_blocks | Compatibility count | Agree | Boot-area (blocks 0–15) fsid hits |
|---|---|---|---|---|---|
| none | 15 + 1 earlier | 367/355 on 15 images, 365/353 on run 6 | identical per image | 16/16 | 0 |
| async | 15 + 1 earlier | 367/355 on all | identical per image | 16/16 | 0 |
| sync | 15 + 1 earlier | 43/31 on all | identical per image | 16/16 | 0 |

- The boot-area supplement was 0 on every image: blocks 0–15 hold no fsid match on any of the 48
  images, and they are all zero bytes on the representative images. So on this corpus the compatibility count equals btrfska's plain full-sweep candidate count and
  its "generation below the superblock" count. The reconciliation still matters in general: an image
  with a foreign or planted fsid in the first 64 KiB would count there for the probe only.
- **Block by block**, the legitimate differences between the probe and btrfska's own classes are:
  - the 5 invalid candidates on every image are mkfs generation-1 blocks at 1081344, 1097728,
    1114112, 1130496 and 1146880 (owners 3, 4, 5, 7, 10). All five lack the WRITTEN flag, and the
    tree-5 leaf is also empty (research.md §10.10). The probe counts them as fsid and stale blocks;
    btrfska reports them as `invalid`, never as orphans;
  - "stale" (generation below the superblock) is not "orphan". On the none/async images, 355 stale =
    350 valid stale copies + the 5 invalid ones; the 350 valid ones classify as 10 `live` (blocks
    unchanged since an older generation, such as fs root generation 19), 24 `backup_reachable` and
    316 `unreferenced`. Of the 12 non-stale (generation 38) copies, 12 are `live`. On sync:
    31 = 26 valid + 5 invalid, of which 10 `live` and 16 `unreferenced`.

### 6.2 btrfska classes per mode (N = 15, full sweep)

| Mode | N | Candidates | Valid | Invalid | Live | Backup-reachable | Unreferenced | Valid orphans outside the map | Legacy-compatible |
|---|---|---|---|---|---|---|---|---|---|
| none | 15 | 367 (365–367) | 362 (360–362) | 5 (5–5) | 22 (22–22) | 24 (24–24) | 316 (314–316) | 172 (170–172) | 355 (353–355) |
| async | 15 | 367 (367–367) | 362 (362–362) | 5 (5–5) | 22 (22–22) | 24 (24–24) | 316 (316–316) | 172 (172–172) | 355 (355–355) |
| sync | 15 | 43 (43–43) | 38 (38–38) | 5 (5–5) | 22 (22–22) | 0 (0–0) | 16 (16–16) | 16 (16–16) | 31 (31–31) |

Next to EXP-000: the candidate counts equal EXP-000's `fsid_blocks` per run, and the only spread is
the none row's run 6 (2 fewer unreferenced `sv1` copies older than generation 30, both outside the
current chunk map).

Walk failures (invalid nodes met by the walks): none on the none and async images; on every sync
image **12 backup-root failures, all `zeroed`**, and none for the current state.

### 6.3 Old-root discovery per mode (N = 15, full sweep)

| Mode | N | Root-tree candidates | Superblock + backup roots indexed (of 26) | … of which candidate roots | States beyond the backups | … complete (every referenced block found) | Backup-state completeness |
|---|---|---|---|---|---|---|---|
| none | 15 | 35 (35–35) | 26 (26–26) | 26 (26–26) | 31 (31–31) | 30 (30–30) | 1 (1–1) over 60 states |
| async | 15 | 35 (35–35) | 26 (26–26) | 26 (26–26) | 31 (31–31) | 30 (30–30) | 1 (1–1) over 60 states |
| sync | 15 | 2 (2–2) | 14 (14–14) | 14 (14–14) | 1 (1–1) | 0 (0–0) | 1 (1–1) over 15 states |

The same result on every image of a mode. On the representative images:

- **none and async.** 35 root-tree states:
  - the 4 backup-root states (generations 35–38, generation 38 also current), each complete (10
    of 10 blocks);
  - **31 states beyond the backups**: generations 3, 6, 7 (two root-tree blocks), and 8–34 (the full
    range, one per transaction); 30 of them have every referenced block found (8–13 blocks each);
  - the generation-3 state has 5 of 6 blocks: one tree root is `unmapped` under the current map.
    That state and generations 6–16 lie in the pre-balance and mkfs chunks, which the current chunk
    map no longer covers. Their inferred historical chunk trees (generations 3, 6, 17, 23, 24, 29,
    30) place every found block where it was scanned.
- **sync.** 2 root-tree states:
  - generation 38 (current and backup 38), complete (10 of 10);
  - generation 3, the same mkfs-era state as above (5 of 6);
  - **lost:** the root, extent, chunk and dev tree roots of backups 35, 36 and 37 (12 roots, 2 copies
    each, all reading as zeros). Their fs root (generation 19) and csum root (generation 35) are
    shared with the live state and survive, hence 14 of 26 roots indexed;
  - the 16 surviving valid orphans are all outside the current chunk map, at 1–1.2 MiB,
    5.0–5.4 MiB, 22020096 and 30408704, with generations 2–5. mkfs finishes at generation 6
    (research.md §10.4), so all of them are mkfs residue. **No block freed by the kernel during the
    scenario survives.**

### 6.4 What the numbers show

- btrfska's full sweep finds exactly the fsid-bearing blocks a byte-level probe finds, on 48 images,
  once each tool's skip ranges and counting rules are made explicit. The M2 DoD agreement bullet is
  met without any tolerance.
- On this scenario, **`discard=sync` removed all history the kernel itself freed**. Without trims,
  old-root discovery resolves 31 root-tree states beyond the 4 backup-root states a
  backup-root-bounded tool can reach, 30 of them with every referenced block found, and every
  backup state is complete. With sync discard, the backup states older than the last commit lose their
  root-tree block, and only mkfs-era residue and the live state remain.
- The kernel code is consistent with this. Under `DISCARD_SYNC`, `btrfs_finish_extent_commit`
  (v7.0 extent-tree.c:2994-3005) discards every extent range unpinned at the end of each
  transaction commit, which includes tree blocks freed by copy-on-write in that transaction. It also
  discards every block group deleted in the transaction (l.3058-3063), such as the chunks the
  balance emptied (block-group.c:1755-1767 freezes them for that trim). A superseded tree block
  therefore survives only until the commit that frees it. This mechanism is read from the source,
  not measured separately.
- Async discard with an unmount about a second after the last write left every block in place in
  all 15 runs (no measurable difference from no discard on any class or discovery metric). That is
  consistent with the async queue delaying its work (`btrfs_discard_schedule_work`,
  extent-tree.c:3048-3051; research.md §10.3).

### 6.5 What the numbers do not show

- **Not file-level recoverability.** Completeness counts tree blocks named by a root-tree state and
  resolved among scanned blocks. It does not read file extents, check data checksums or verify
  content (M4, M6). On these small images every root tree is a single leaf and most trees are one
  block deep, so completeness is easy to reach; deeper trees will expose more missing blocks.
  Nothing is known below a missing block, so completeness is an upper bound.
- **Not SSD behaviour.** TRIM here punches holes in a sparse raw file on ext4 through virtio-blk,
  so trimmed blocks read as zeros. A real SSD may return zeros, old data or indeterminate data for a
  trimmed LBA, and chip-off acquisition may still see flash contents. "Zeroed" is a statement about
  the image a host acquires through such a block layer.
- **Not async discard in general.** Only a quick unmount was measured; an idle filesystem trims
  asynchronously and would lose history too (plan.md M7 "async idle" row).
- **Not other scenarios, sizes or kernels.** One 512 MiB scenario with 38 transactions and a full
  balance, stock 7.0 guest, host mkfs 6.6.3. The survival of 31 states without trims reflects a
  quiescent image; later writes would reuse many of those blocks (`reused` in walk failures, as on
  `m2_logtree`).
- **Not the "no discard" mount option.** The none row's guest mounts with `discard=async`; the host
  drops the TRIMs (EXP-000 §3).
- **Not the cause of the run-6 jitter**, nor whether rarer outcomes exist beyond 15 runs.

## 7. Threats to validity

- **Internal.** The agreement is a parse of fixed images, repeated on 48 images; a shared bug in
  offset iteration would affect both counts only if the probe and btrfska shared code, and they
  share none (the probe is standard-library Python, btrfska uses numpy strided views). Discovery
  results are identical within each mode, so run-to-run guest jitter did not reach them here.
- **External.** See §6.5: virtio sparse-file TRIM semantics, one small scenario, stock kernel, no
  aging, quick unmount.
- **Construct.** "Root-tree state" means an owner-1 candidate root, which may be an older leaf of a
  multi-leaf root tree whose parent is gone (README, `btrfska roots`). On these images every
  root-tree block is a level-0 root, so each state is a whole root tree. "Complete" is relative to
  what the found blocks reference.

## 8. Status

- **Agreement: supports.** 48 of 48 images agree exactly (M2 DoD met).
- **Discard and history: observation**, deterministic per mode across 15 regenerations. Sync
  discard left 2 of 35 root-tree states (the live one and mkfs residue), 0 backup-reachable blocks
  and 14 of 26 superblock and backup roots; none and async left all 35 states, 31 of them beyond the
  backup roots.
- **Follow-ups:** the async-idle and `nodiscard` rows (M7); deeper trees and file-level recovery
  per state (M4/M5); a real-SSD acquisition; historical chunk maps for the pre-balance states (M5).
