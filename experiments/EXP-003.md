# EXP-003 — Scan kernel throughput on a synthetic 10 GiB image

- **Date:** 2026-09-15
- **Milestone:** M2 DoD "≥200 MB/s single-core on a synthetic 10 GiB image"
  (plan.md §5), catalog.md M2a entry
- **Script:** [`experiments/bench_scan.py`](bench_scan.py); environment:
  [`experiments/env.sh`](env.sh)
- **Raw outputs:** `images/scratch/exp/EXP-003/` (gitignored): `env.txt`,
  `image.json`, `results.json`, `stdout.txt` and `profile_full.txt`. The
  image itself was deleted after the runs (`clean`); `generate` rebuilds it
  byte for byte.
- EXP-002 is reserved for the discard trio (M2b).

## 1. Hypothesis

On one core, the numpy scan kernel scans a 10 GiB image whose tree-block
density resembles a filesystem's (about 2 % of the image) at ≥ 200 MB/s. This
holds for the prefilter alone and for full validation, with a cold and with a
warm page cache.

## 2. Method

- **Kernel under test.** `btrfska.scan.kernel_numpy` at commit `7571f89`,
  `workers=1` (one process), over one region covering the whole image, probing
  every 4 KiB offset.
  - **prefilter**: `iter_prefilter_hits`. Two strided u64 compares per window
    of 65 536 offsets through a view of the read-only map.
  - **full**: `iter_candidate_nodes`. The prefilter, then for every hit a
    16 KiB copy, `check_block` (csum and every header and layout check) and a
    chunk-map lookup. The chunk map is empty, so every lookup misses.
- **Cache states.**
  - **cold**: `posix_fadvise(POSIX_FADV_DONTNEED)` on the image before the
    run. It needs no root, because the benchmark owns the file.
  - **warm**: the run follows one that has just read every page.
  - The fraction of the image's pages in the page cache is measured with
    `mincore()` right before each timed run, not assumed.
- **Schedule.** N = 5 rounds. Each round runs cold prefilter, warm
  prefilter, cold full, warm full, in that order. Every run opens a fresh
  image handle (a fresh map), so each run pays its own page faults. The
  process was not pinned (affinity: all 12 logical CPUs).
- **Metrics.**
  - Throughput is image bytes divided by wall-clock seconds, in MB/s
    (10^6 B/s, the DoD's unit) and MiB/s.
  - CPU seconds are `time.process_time()`.
  - Each run also records its hit and valid counts, which must equal the
    generator's (11 151 hits; 11 151 − 106 corrupted = 11 045 valid).
- **Success.** Every cell's median is ≥ 200 MB/s and every run's counts
  equal the generator's.
- **Bottleneck.** `profile --mode full` runs one warm full scan under
  cProfile.

## 3. Image

The image is synthetic, not from `corpus/vm`, so it has no `corpus/manifest.tsv`
row. `bench_scan.py generate` builds it deterministically from these parameters
(`image.json`):

| Parameter | Value |
|---|---|
| size | 10 737 418 240 B (10 GiB), 160 tiles of 64 MiB |
| seed | 3 (Python `random.Random`) |
| metadata tiles | 6: 16 KiB slots, each filled with probability U(0.2, 0.9) drawn per tile |
| data tiles | 80: random bytes |
| hole tiles | 74: never written (sparse) |
| node pool | the 84 valid scan candidates of `sandbox.img` (crc32c, nodesize 16 KiB); pool SHA-256 `40cf88e93eacfe100bfe55878738dec96ab9d20eabd45bac2e1474670a702602` |
| corrupted copies | 1 % target: one byte flipped after the header fsid, so a copy stays a candidate and fails its csum |
| tree blocks written | 11 151 (106 corrupted) = 174 MiB, 1.6 % of the image |
| allocated on disk | 5 771 366 400 B |
| image SHA-256 | `acf9b2ef712d3c78564888e0e1b0efc04db4129982b5d64c261e4c9424bc25e1` |

- Copied blocks keep valid checksums: a tree block's checksum does not
  cover its physical location.
- Generation took 28.0 s. `generate --verify` recomputed the same SHA-256
  without writing (26.2 s wall).
- `sandbox.img` was only read, and its hash is unchanged (catalog.md
  verification table).

## 4. Environment record

Output of `experiments/env.sh sandbox.img`, taken immediately before the run
(the QEMU and guest lines are irrelevant here; no guest runs):

```
date_utc: 2026-09-15T02:33:04Z
host_cpu: 13th Gen Intel(R) Core(TM) i5-1335U
host_logical_cpus: 12
host_ram: 15.3 GiB
images_storage: /dev/nvme0n1p2 (ext4) on /dev/nvme0n1, model KINGSTON OM8SEP4512Q-AA, rotational 0
host_kernel: 7.0.0-31-generic
qemu: QEMU emulator version 8.2.2 (Debian 1:8.2.2+ds-0ubuntu1.18)
guest_kernel: 7.0.0-31-generic
host_btrfs_progs: mkfs.btrfs, part of btrfs-progs v6.6.3
guest_btrfs_progs: Package: btrfs-progs Version: 6.6.3-1.1build2 
python: 3.14.6
uv: uv 0.11.28 (x86_64-unknown-linux-gnu)
uv_lock_sha256: 08dc30c689798ef3014e2dfc2c7098cc0e38ebc409275c697a88bf35eb58f097
git_commit: 7571f89ae1f62e28da985dcbb0d0e9de3ecb0a72
git_dirty_tracked_files: 0
image_sha256: 07ca38d42b11062f5461f97a572134a1b56cbf94e1138183d6e74502f5876418  sandbox.img
```

Other dependencies: numpy 2.5.3, crc32c from `uv.lock`. `MemAvailable` was
12.0 GiB before and after the run.

## 5. Exact commands

```sh
uv run python experiments/bench_scan.py generate
experiments/env.sh sandbox.img > images/scratch/exp/EXP-003/env.txt
uv run python experiments/bench_scan.py run --runs 5
uv run python experiments/bench_scan.py profile --mode full
uv run python experiments/bench_scan.py generate --verify
uv run python experiments/bench_scan.py clean
# M2a review additions
uv run python experiments/bench_scan.py allocated          # §6, per non-hole byte
uv run python experiments/bench_scan.py sweep-generate     # §6.1 images
experiments/env.sh sandbox.img > images/scratch/exp/EXP-003/env_sweep.txt
uv run python experiments/bench_scan.py sweep-run --runs 5
uv run python experiments/bench_scan.py clean              # deletes the sweep images too
```

## 6. Results

N = 5 per cell. Entries are median (min–max). The whole `run` took 52.8 s
wall, of which 5.8 s user and 27.5 s system CPU (shell `time`, including the
untimed node-pool scan).

| Mode | Cache | N | MB/s | MiB/s | Wall s | CPU s | Pages cached before the run | Hits | Valid |
|---|---|---|---|---|---|---|---|---|---|
| prefilter | cold | 5 | 3743.1 (3505.4–4328.8) | 3569.7 (3343.0–4128.3) | 2.87 (2.48–3.06) | 1.73 (1.31–1.86) | 0.0000 (0.0000–0.0000) | 11151 | — |
| prefilter | warm | 5 | 6138.6 (3622.4–8364.7) | 5854.2 (3454.5–7977.2) | 1.75 (1.28–2.96) | 0.88 (0.55–1.78) | 0.8978 (0.8171–0.9259) | 11151 | — |
| full | cold | 5 | 3277.1 (3227.5–3337.8) | 3125.2 (3078.0–3183.2) | 3.28 (3.22–3.33) | 2.11 (2.01–2.14) | 0.0000 (0.0000–0.0000) | 11151 | 11045 |
| full | warm | 5 | 5691.0 (4430.1–7025.2) | 5427.3 (4224.9–6699.8) | 1.89 (1.53–2.42) | 1.07 (0.89–1.18) | 0.9092 (0.8735–0.9280) | 11151 | 11045 |

- **Per non-hole byte** (M2a review). 4.97 GiB of the image are holes,
  which read as zeros without device I/O, so image bytes per second
  overstate the rate at which data comes off the device. The same runs,
  divided by the 5 771 366 400 allocated bytes (`bench_scan.py allocated`,
  from `results.json` and `image.json`):

  | Mode | Cache | N | Image MB/s | Allocated (non-hole) MB/s |
  |---|---|---|---|---|
  | prefilter | cold | 5 | 3743.1 (3505.4–4328.8) | 2011.6 (1884.2–2327.2) |
  | prefilter | warm | 5 | 6138.6 (3622.4–8364.7) | 3299.8 (1947.2–4494.8) |
  | full | cold | 5 | 3277.1 (3227.5–3337.8) | 1761.2 (1734.7–1794.0) |
  | full | warm | 5 | 5691.0 (4430.1–7025.2) | 3058.5 (2380.9–3777.1) |

- **DoD met.** The slowest single run (full, cold) reached 3 227.5 MB/s of
  image, 1 734.7 MB/s of allocated bytes, 8.7× the 200 MB/s target. Every
  run found exactly the generator's 11 151 blocks and 11 045 valid ones.
- **Warm runs were not fully cached.** Between 7 % and 18 % of the pages had
  been reclaimed before each warm run: the 10 GiB cache competes for 15.3 GiB
  of RAM. The profiled run started with 99.88 % of pages cached and took
  0.883 s wall, 0.849 s CPU (12 156.7 MB/s). The warm medians therefore
  understate a fully cached scan, and their wide range tracks the cached
  fraction.
- **Bottleneck.**
  - **Cold.** Reading the 5.8 GiB of allocated blocks from NVMe: wall exceeds
    CPU by 1.1–1.2 s in both cold cells.
  - **Overall.** Page-fault and page-cache work in the kernel. System CPU
    (27.5 s) was almost five times user CPU (5.8 s) over the whole run,
    including the cache drops and refaults that the cold runs force.
  - **In the process** (profile, fully cached, 0.95 s under cProfile):
    - per-candidate work in `_record` takes 0.680 s cumulative, of which
      `check_block` is 0.536 s and its per-item layout checks 0.197 s. That
      is about 61 µs per candidate, in Python;
    - the numpy prefilter (`_window_hits`, 40 windows) takes 0.185 s;
    - crc32c itself takes 0.014 s.
  - **Scaling.** Validation cost grows with the number of candidates, the
    prefilter's with image size. No optimisation was needed for the DoD.

### 6.1 Density sweep (M2a review)

The review asked for the validation bound to be measured, not extrapolated.
It had reported single runs of 262 MB/s cold and 287 MB/s warm at 100 %
density, and 803 and 2 384 MB/s at 10 %. Those runs were on sparse images
with empty slots left as holes. The table below replaces them.

- **Images** (`bench_scan.py sweep-generate`, `sweep_images.json`). Three
  1 GiB images at 0 %, 10 % and 100 % density.
  - Each 16 KiB slot holds a copy of a `sandbox.img` tree block (the §3 node
    pool) with probability density %; every other slot is random bytes. The
    images are dense: allocated bytes equal the size, so image bytes per
    second are device bytes per second.
  - 1 % of the copies are corrupted after the fsid, as in §3. Seeds are 1000
    plus the density.
  - Blocks: 0; 6 476 (66 corrupt); 65 536 (631 corrupt).
  - SHA-256: `40230c1c…5517`, `3825d5b5…e4f6`, `0303aaa6…519a` (full values
    in `sweep_images.json`).
- **Runs** (`bench_scan.py sweep-run --runs 5`, `sweep_results.json`).
  - Full validation, one process, runs sequential.
  - Each round runs cold then warm for 0 %, 10 % and 100 %.
  - Cold is `POSIX_FADV_DONTNEED`; `mincore()` confirmed 0 cached pages
    before every cold run and all pages cached before every warm run.
  - Device bytes read are `/proc/self/io` `read_bytes` over the run.
  - Every run found exactly the generator's hits and valid blocks.
- **Environment.** `env_sweep.txt` records the same host as §4, at
  `d4c2c59` with 16 tracked files modified (the uncommitted review fixes).
  The measured path, `iter_candidate_nodes` with one worker, is unchanged
  by them except one `if expect.log` branch in `check_block`. `MemAvailable`
  was 11.9 GiB before and after. The `sweep-run` took 25.1 s wall.

N = 5 per cell, median (min–max):

| Density | Cache | MB/s (image = device bytes) | Wall s | CPU s | Device read MB | Hits | Valid |
|---|---|---|---|---|---|---|---|
| 0 % | cold | 3451.7 (3362.5–3456.7) | 0.31 (0.31–0.32) | 0.11 (0.11–0.14) | 1073.7 | 0 | 0 |
| 0 % | warm | 65669.9 (55699.1–68240.6) | 0.02 (0.02–0.02) | 0.02 (0.02–0.02) | 0.0 | 0 | 0 |
| 10 % | cold | 2142.5 (2069.7–2159.2) | 0.50 (0.50–0.52) | 0.32 (0.32–0.33) | 1073.7 | 6476 | 6410 |
| 10 % | warm | 5685.7 (5056.6–6037.6) | 0.19 (0.18–0.21) | 0.19 (0.18–0.21) | 0.0 | 6476 | 6410 |
| 100 % | cold | 512.2 (501.0–529.7) | 2.10 (2.03–2.14) | 1.88 (1.85–1.94) | 1073.7 | 65536 | 64905 |
| 100 % | warm | 615.9 (576.0–628.2) | 1.74 (1.71–1.86) | 1.74 (1.71–1.86) | 0.0 | 65536 | 64905 |

- **The validation bound is about 27 µs per candidate** on this host (1.74 s
  CPU for 65 536 candidates, warm). That is 2.3× better than §6's
  61 µs/candidate profile, which ran under cProfile. A 100 % metadata image
  still scans at 512 MB/s cold, 2.6× the DoD.
- **At 0 %** the cold rate is the device read rate (about 3.45 GB/s); warm,
  the numpy prefilter alone does 65 GB/s.
- **Against the review's single runs:** 100 % density is about 2× faster
  here (512 and 616 against 262 and 287 MB/s). At 10 %, cold is 2 143
  against 803 MB/s and warm 5 686 against 2 384 MB/s. Causes not
  controlled: the review's runs were single and unpinned, on sparse images,
  and possibly under other load.

## 7. Threats to validity

- **Internal.**
  - Timing jitter on a hybrid CPU (P and E cores, process not pinned) and
    partial page-cache reclaim in warm runs; the cached fraction is reported
    per run.
  - The cold state was produced by `POSIX_FADV_DONTNEED`, not a reboot.
    `mincore()` confirmed 0 cached pages, but device-level caches were not
    controlled.
  - `hits` and `valid` equal the generator's counts in all 20 runs, so the
    speed was not bought by skipping work.
- **External.**
  - The image is sparse: 4.97 GiB of holes read as zeros without device
    I/O, so a dense image of the same size needs more read time when cold.
  - The storage is one consumer NVMe drive on ext4. HDDs or network storage
    will be I/O-bound far sooner (plan.md §9).
  - Tree blocks come from one image (crc32c, 16 KiB); other checksum types
    cost more per candidate (sha256, blake2b).
- **Construct.**
  - Tree-block density was 1.6 % of the image. Per-candidate validation is
    the largest in-process cost, so a metadata-dense image is slower.
  - The §6.1 density sweep measures the dense case directly: 512 MB/s cold
    and 616 MB/s warm at 100 % tree blocks. That replaces the earlier
    extrapolation of about 270 MB/s from the profiled 61 µs per candidate.
  - The empty chunk map makes each lookup a bisect miss. Real maps with many
    chunks cost more per candidate.

## 8. Status

**Supports** the hypothesis. The M2 performance DoD is met with a large margin
in all four cells, per image byte and per allocated byte, and at every
density of the §6.1 sweep (slowest cell 501.0 MB/s, 100 % cold). Follow-ups:
- a pinned-core run on a P core;
- a non-sparse image of 10 GiB on the same drive (the sweep's 1 GiB images
  are non-sparse);
- multi-worker scaling, which is not measured by default because of the
  host's CPU-load limit.
